function About() {
  return (
    <div className="page-card">
      <h2>About Page</h2>
      <p>This app uses client-side routing, so pages change without reloading.</p>
      <p>React Router helps manage different views inside one React app.</p>
    </div>
  );
}

export default About;
